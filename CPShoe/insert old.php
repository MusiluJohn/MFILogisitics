<?php 
$id= $_GET['id'] ?? '';
$pin= $_GET['pin'] ?? '';
$invno= $_GET['invno'] ?? '';
$invdate= $_GET['invdate'] ?? '';
$taxrate= $_GET['taxrate'] ?? '';
$invvat= $_GET['invvat'] ?? 0;
$inc_amt= $_GET['inc_amt'] ?? 0;
$vat_w= $_GET['vat_W'] ?? 0;
$gr_tot= $_GET['gr_tot'] ?? 0;
$nva= $_GET['nva'] ?? 0;
$inc_withheld= $_GET['inc_withheld'] ?? 0;
$amt_payable= $_GET['amt_payable'] ?? 0;
$paymentdate= $_GET['paymentdate'] ?? '';
$curr= $_GET['curr'] ?? '';
$exchrate= $_GET['exch_rate'] ?? 0;

//check if exists


//if exists update.

//if not insert based on current batch no.

//check if batch has 10 items

//if yes create new batch

//if no update to existing batch

?>